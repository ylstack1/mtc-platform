{
  "name": "cf-cms-template-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace=apps/admin-frontend",
    "build:frontend": "npm run build --workspace=apps/admin-frontend",
    "lint:frontend": "npm run lint --workspace=apps/admin-frontend"
  }
}
